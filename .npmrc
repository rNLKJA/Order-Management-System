# pnpm monorepo + Expo / React Native 官方兼容方案
# node-linker=hoisted 让 pnpm 用 flat node_modules（类 npm/yarn 行为）。
# 这是 Expo 官方文档对 pnpm 的推荐配置：
# https://docs.expo.dev/guides/monorepos/#pnpm
node-linker=hoisted
